{
  "name": "donarte",
  "version": "0.0.0",
  "private": true,
  "engines": {
  "node": "12.16.1",
  "npm": "6.14.8"
  },
  "scripts": {
    "start": "nodemon ./bin/www",
    "dev": "concurrently --kill-others-on-fail \"npm run back\" \"npm run front\"",
    "heroku-postbuild": "cd front && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "^4.16.4",
    "mongodb": "^3.6.2",
    "morgan": "~1.9.1",
    "nodemon": "^2.0.6",
    "react": "^17.0.1"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
